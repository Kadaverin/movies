import { makeStyles } from '@material-ui/core';

export const useDiscoverPanelStyles = makeStyles({
  select: {
    minWidth: '140px',
  },

  grow: {
    flexGrow: 1,
  },

  genresSelect: {
    flexGrow: 1,
  },

  container: {
    padding: '2rem 1rem',
  },
});
